#pragma once
// Trapezoid Rule
#include <utility>
#include <string>

std::pair<double, double> FirstBounds(1,2);
std::string Expected;
std::pair<double, double> SecondBounds(-1, -2);
std::pair<double, double> ThirdBounds(154, 1);